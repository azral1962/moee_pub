```{r}
#| label: tbl-metrics
#| tbl-cap: "Validation, value-creation, and resilience metrics"
#| tbl.env: tabular
#| fig-pos: h
#| echo: false

options(knitr.table.format = function() {
  if (knitr::is_latex_output()) "latex" else "pandoc"
})

csv_candidates <- c("tables/table_17_evaluation_metrics.csv", "../tables/table_17_evaluation_metrics.csv", "table_17_evaluation_metrics.csv")
csv_file <- csv_candidates[file.exists(csv_candidates)][1]
if (is.na(csv_file)) stop("Cannot find table_17_evaluation_metrics.csv")
dt <- read.csv(csv_file, check.names = FALSE, encoding = "UTF-8")

knitr::kable(dt, booktabs = TRUE, row.names = FALSE)
```
