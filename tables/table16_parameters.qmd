```{r}
#| label: tbl-parameters
#| tbl-cap: "Simulation and MIG-reasoning parameters"
#| tbl.env: tabular
#| fig-pos: h
#| echo: false

options(knitr.table.format = function() {
  if (knitr::is_latex_output()) "latex" else "pandoc"
})

csv_candidates <- c("tables/table_16_simulation_parameters.csv", "../tables/table_16_simulation_parameters.csv", "table_16_simulation_parameters.csv")
csv_file <- csv_candidates[file.exists(csv_candidates)][1]
if (is.na(csv_file)) stop("Cannot find table_16_simulation_parameters.csv")
dt <- read.csv(csv_file, check.names = FALSE, encoding = "UTF-8")

knitr::kable(dt, booktabs = TRUE, row.names = FALSE)
```
